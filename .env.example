# Database Configuration
# Your Postgres database connection string (required)
# For Vercel Postgres: Get this from your Vercel project settings
POSTGRES_URL=postgresql://user:password@host:5432/database

# Payload CMS Configuration
# Secret key for Payload CMS (required)
# Generate with: openssl rand -base64 32
PAYLOAD_SECRET=your-secret-key-here

# Vercel Blob Storage
# Token for Vercel Blob Storage (required for media uploads)
# Get this from your Vercel project settings
BLOB_READ_WRITE_TOKEN=your-blob-token-here

# Vercel Cron Jobs
# Secret for authenticating Vercel Cron requests (optional but recommended)
# Generate with: openssl rand -base64 32
CRON_SECRET=your-cron-secret-here

# Application Configuration
# The public URL of your application
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Environment (set to 'production' for production deployments)
NODE_ENV=development
